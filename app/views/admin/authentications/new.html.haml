%div.login
  %header
    %h1 Log in
  = form_tag(admin_authentication_path, :method => :post) do
    %fieldset
      = label_tag('password', 'Enter password')
      = password_field_tag('password')
      = submit_tag('Log in', :class => 'button primary')
